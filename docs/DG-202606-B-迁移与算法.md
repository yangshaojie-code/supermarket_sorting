# B 线：迁移与算法实现

负责人：ROS 控制、导航、抓放、任务编排接到真机动作。代码在零售仓库（当前 `D:\DG\supermarket_sorting`）。

更新：2026-09-01。

新 Agent 先读 [AGENTS.md](./AGENTS.md)。和 A 的边界、9 类名字、合并节点见 [DG-202606-分工总览.md](./DG-202606-分工总览.md)。

## 当前情况

平台层已经从文旅思路迁了一部分到零售仓库，**任务层不能用文旅三步重排/双臂 hug/HSV 色箱**。

| 模块 | 路径 | 状态 |
| --- | --- | --- |
| 话题契约 | `runtime/ros_contract.py` | 完成 |
| 限幅控制 | `runtime/ros_robot_control.py` | 完成：底盘 clip ±0.35/±0.65，`stop_all` |
| 传感器 | `runtime/ros_sensor_utils.py` | 完成 |
| 头相机 FK | `runtime/head_camera_kinematics.py` | 完成 |
| IK 封装 | `runtime/motion_planning.py` → `kinematics.mmk2_kdl` | 完成 |
| 预检 | `scripts/run_preflight.sh` | **仿真已通过** |
| 任务 JSON | `runtime/task_protocol.py` | 完成；忽略未下发坐标 |
| 取送状态机 | `runtime/orchestrator.py` | 完成但 **还没有接运动** |
| 区域 | `runtime/scene_zones.py` | 完成（送货台、拣货区、420 s） |
| P2 干跑 | `scripts/run_p2_preview.sh` | 1× kele → DONE |
| ArUco + 绑槽 | `perception/aruco_slots.py` | 完成；例：id 32 → D-L2-C3 |
| P3 预览 | `scripts/run_p3_preview.sh` | 货架前、GS=1 slim 可乐：能解码 + 认出 kele 并绑槽。YOLO 必须 CPU |
| 官方可乐演示 | `client_task_1.py` | 保留作参考，**不要改成评分主程序** |
| 雷达导航 | `runtime/p4_nav.py` + `lidar_avoid.py` | P4：通道航点 + 雷达侧向绕障。`scripts/run_p4_nav.sh --goal shelf\|delivery` |
| 面向货架扫描 | — | **未做** |
| 真抓真放 | — | **未做**（演示脚本里有一次可乐抓取可参考） |
| 9 类 YOLO | `weights/kele.pt` | 等 A；算法线继续用可乐打通动作 |

仿真补丁：Server 启动需挂载

`supermarket_sorting_baseline/patches/simulator.py` → 容器内 `discoverse/envs/simulator.py`

原因：`screeninfo` 没有 `is_primary`。正式提交是否仍要该补丁，以赛方镜像为准；本机调试请保持挂载。

## 任务目标

在 **不改官方评分协议** 的前提下，让 Client 真正执行：收任务 → 去货架看见目标 `kind` → 抓起 → 送到配送桌 → 下一件。失败不得重置 Server。

交付物：

- 评分入口（新建，例如 `runtime/mission_client.py` + `scripts/run_mission.sh`），不要把逻辑堆回 `client_task_1.py`
- 底盘：拣货区 ↔ 送货接近点 `DELIVERY_APPROACH_XY = (-1.940, -2.90)`，雷达绕开通道障碍
- 头/升降：扫到 ArUco 0–44 中与当前目标相关的货架
- 手臂：参考官方可乐抓取，经现有限幅接口发出；一次只抓任务里的当前 `kind`
- 时间：仿真时钟 420 s（`scene_zones.TIME_LIMIT_S`）

## 建议顺序（权重未到之前就能做）

### 1. 让 P3 不再空（已完成 2026-09-01）

车可开到拣货区，`GS=1` slim 可乐 + 背景时 P3 能绑 `kele`。A 的采图必须用同一套头相机 GS 外观；4060 分批 ply 见 [A 线文档](./DG-202606-A-数据采集与YOLO.md)。

下一步改为：对已绑定槽位做一次真抓真放。

### 2. 雷达导航（P4，已落地代码）

话题：`/slamware_ros_sdk_server_node/scan`（360 点，`laser`）。  
沿黄线航点走，前方过近则减速/停车，堵死则向更空的一侧插入绕障点。**不做全图搜索。** 控制走 `RosRobotController`。

```bash
bash scripts/run_p4_nav.sh --goal shelf
bash scripts/run_p4_nav.sh --goal delivery
```

进度行和最终 JSON 写到 `outputs/p4/{goal}_{时间戳}.log/.json`，以及 `outputs/p4/last.json`。不要为了重跑而重启 Server：脚本从当前 odom 裁掉已经经过的航点。东通道在 y≈2.0 西转，不要冲到货架北墙。回程在 y≈-3.22 从隔墙南端绕到配送区，不要在 y=-2.90 对着隔墙西开。

`run_drive_to_shelf.sh` 已改走同一套 P4。固定障碍可先 `SUPERMARKET_RANDOMIZE_OBSTACLES=0`；正式跑再开随机 5 箱。

下一步：对已绑定槽位做一次真抓，再用 `--goal delivery` 送回配送接近点 `(-1.940, -2.90)`。回程先南撤到 y=2.0，东转到东通道，再南下到 y≈-3.22 绕过隔墙，不要在 y=-2.90 对着隔墙西开。

### 3. 把编排接到动作

`MissionOrchestrator` 已约束「取一件必须送一件」。B 要补的是 `start_pick` / 送达成功或失败时的 **真实运动回调**，而不是再写一套状态机。

送达判定对齐裁判区域：`DELIVERY_BOX_ZONE`，桌面约 `(-1.940, -3.410)`，目标高度 0.807 m。搬离阈值等见 `scene_zones.py`。

失败：记录原因，从当前物理状态重观测，禁止重启容器/改种子「重来」。

### 4. 抓取（P5）

可阅读 `client_task_1.py` 的可乐流程，把可用段迁到 `runtime/` + 限幅控制器。双臂 hug、文旅站位配方不要用。

深度 + 头相机 FK 已经在 P1；抓空时用 P3 的槽位绑定重试，不要猜 JSON 坐标。

### 5. A 交出 9 类权重之后（两人一起）

- `YoloBackend.CLASS_NAMES` 改为总览中的 9 个 `kind`
- 默认权重改指向 `weights/supermarket9.pt`（或 A 指定路径）
- 任务 `kind` 过滤：只抓当前目标类，不要见瓶就抓
- 再开 `RANDOMIZE=1` 与障碍随机

## 明确不要做

- 重写或删除 `client_task_1.py` 当唯一入口
- 把 `vlm_pipeline` 的 Task1/2/3、DashScope、HSV 色箱检测拷进零售评分
- 在评分路径读取未下发的商品世界坐标
- 升级 Client 里的 ultralytics / 引入 Genesis World
- 和 A 同时改 `yolo_backend.py` 的类别表（等 A 的 `data.yaml` 定稿）
- `source /opt/ros/humble/setup.bash` 前 `set -u`（会踩 `AMENT_TRACE_SETUP_FILES`）

## B 的完成标准（可叫 A 一起联调）

- [ ] 固定布局下能从出生位走到货架，P3 预览出现 ArUco
- [ ] 雷达在通道障碍下能绕开或停车，不长期顶死（`run_p4_nav.sh`）
- [ ] 用 `kele.pt` 完成至少 **1 次** 取可乐并放到配送桌（裁判认可或目视稳定）
- [ ] 超时与 `stop_all` 在异常时会停
- [ ] 评分入口脚本可一键在 Client 内启动

权重到位后的共同目标：同一入口换 9 类模型，尽量多完成取送循环，而不是再分叉两套 Client。
